# Создать текстовый файл (не программно),
# построчно записать фамилии сотрудников и величину их окладов.
# Определить, кто из сотрудников имеет оклад менее 20 тыс.,
# вывести фамилии этих сотрудников.
# Выполнить подсчет средней величины дохода сотрудников.

low_sal = []
sum_of_sal = 0
num_of_units = 0
with open("third_file.txt", "r") as thrd_file:
    for line in thrd_file:
        print(line)
        analytics = line.split(":")
        if int(analytics[1]) < 20000:
            low_sal.append(analytics[0])
            sum_of_sal += int(analytics[1])
            num_of_units += 1
result = sum_of_sal / num_of_units
print(f"Сотрудники, получающие менее 20 тыс. : {low_sal} \n"
      f"Средняя заработная плата составляет: {result}")
