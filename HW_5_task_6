# Необходимо создать (не программно) текстовый файл,
# где каждая строка описывает учебный предмет и наличие лекционных,
# практических и лабораторных занятий по этому предмету и их количество.
# Важно, чтобы для каждого предмета не обязательно были все типы занятий.
# Сформировать словарь, содержащий название предмета и общее количество занятий по нему.
# Вывести словарь на экран.
# Примеры строк файла:
# Информатика: 100(л) 50(пр) 20(лаб).
# Физика: 30(л) — 10(лаб)
# Физкультура: — 30(пр) —
#
# Пример словаря:
# {“Информатика”: 170, “Физика”: 40, “Физкультура”: 30}

import re


def remove_text_between_parens(text):
    n = 1
    while n:
        text, n = re.subn(r'\([^()]*\)', '', text)  # задача функции - удалить скобки и все что в них вложено
    return text


education = {'Предмет': 'subj', 'Лекции': 'lecture', 'Практика': 'practice', 'Лабораторные': 'lab'}

with open("six_file.txt", "r", encoding='utf-8') as six_f:
    for line in six_f:
        frst_step = remove_text_between_parens(line)
        subj, lecture, practice, lab = frst_step.split()
        if lecture == "—":
            lecture = 0
        elif practice == "—":
            practice = 0
        elif lab == "—":
            lab = 0
        education.update({"Предмет": subj, "Лекции": lecture, "Практика": practice, "Лабораторные": lab})
        print(education)
        if lecture == "—":
            lecture = 0
        elif practice == "—":
            practice = 0
        elif lab == "—":
            lab = 0
        education[subj] = int(lecture) + int(practice) + int(lab)
print(f"Количество часов по предметам: \n {education}")

# на наведение красоты, времени не хватило...
