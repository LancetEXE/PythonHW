# Реализовать функцию, принимающую два числа (позиционные аргументы) и выполняющую их деление.
# Числа запрашивать у пользователя, предусмотреть обработку ситуации деления на ноль.


def math_func(*args):
    try:
        var_1 = input("Введите делимое число: ")
        var_2 = input("Введите делитель число: ")
        if var_2 != 0:
            return print(f"Результат деления: {int(var_1) / int(var_2)}")
    except ValueError:
        return print("Введенное значение не распознано, попробуйте еще раз")
    except ZeroDivisionError:
        return print("на 0 делить нельзя!")


math_func()
