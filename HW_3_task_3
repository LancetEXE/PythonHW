#  Реализовать функцию my_func(), которая принимает три позиционных аргумента,
#  и возвращает сумму наибольших двух аргументов.

def my_func(var_1, var_2, var_3):
    """Функция принимает 3 позиционных значения и возвращает сумму наибольших 2х путем удаления наименьшего из списка
    но! другое решение (закоментированное ниже) мне нравится больше - оно предусматривает введение
    исключения - распознает ошибку при введени символов, данная программа этого не позволяет сделать, не пойму почему
    """

    try:
        user_list = [var_1, var_2, var_3]
        user_list.remove(min(var_1, var_2, var_3))
        return (sum(user_list))
    except ValueError:
        return print("Введенное значение не распознано, попробуйте еще раз")


print(f"Сумма из 2х наибольших чисел: {my_func(int(input('Введите первое число: ')), int(input('Введите второе число: ')), int(input('Введите третее число: ')))}")

"""
def sum_of_max(*args):
    try:
        var_1 = int(input("Введите первое число: "))
        var_2 = int(input("Введите второе число: "))
        var_3 = int(input("Введите третее число: "))
        user_list = [var_1, var_2, var_3]
        user_list.remove(min(var_1, var_2, var_3))
        print(f"Сумма из 2х наибольших чисел составляет: {sum(user_list)}")
    except ValueError:
        return print("Введенное значение не распознано, попробуйте еще раз")


sum_of_max()"""