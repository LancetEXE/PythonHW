# Реализовать проект расчета суммарного расхода ткани на производство одежды.
# Основная сущность (класс) этого проекта — одежда, которая может иметь определенное название.
# К типам одежды в этом проекте относятся пальто и костюм.
# У этих типов одежды существуют параметры: размер (для пальто) и рост (для костюма).
# Это могут быть обычные числа: V и H, соответственно.
# Для определения расхода ткани по каждому типу одежды
# использовать формулы: для пальто (V/6.5 + 0.5), для костюма (2 * H + 0.3).
# Проверить работу этих методов на реальных данных.
# Реализовать общий подсчет расхода ткани.
# Проверить на практике полученные на этом уроке знания:
# реализовать абстрактные классы для основных классов проекта,
# проверить на практике работу декоратора @property.

class Clothes:
    def __init__(self, width, height):
        self.width = width
        self.height = height

    def get_coat_textil(self, coat_result):
        self.coat_result = coat_result
        coat_result = self.width / 6.5 + 0.5
        return coat_result

    def get_suit_textil(self, suit_result):
        self.suit_result = suit_result
        suit_result = 2 * self.height + 0.3
        return suit_result

    @property
    def sum_textil(self):
        return str(f"Общая площадь необходимой ткани: \n"
                   f"{round((self.width / 6.5 + 0.5) + (2 * self.height + 0.3))} м.кв")

class Coat(Clothes):
    def __init__(self, width, height):
        super().__init__(width, height)
        self.coat_result = round(self.width / 6.5 + 0.5)

    def __str__(self):
        return f"Площадь необходимой ткани для пальто составляет: {self.coat_result} м.кв"

class Suit(Clothes):
    def __init__(self, width, height):
        super().__init__(width, height)
        self.suit_result = round(2 * self.height + 0.3)

    def __str__(self):
        return f"Площадь необходимой ткани для костюма составляет: {self.suit_result} м.кв"



some_suit = Suit(3, 5)
some_coat = Coat(3, 6)
print(some_suit)
print(some_coat)
print(some_coat.sum_textil)
print(some_suit.sum_textil)
