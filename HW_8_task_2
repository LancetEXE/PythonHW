# Создайте собственный класс-исключение, обрабатывающий ситуацию деления на нуль.
# Проверьте его работу на данных, вводимых пользователем.
# При вводе пользователем нуля в качестве делителя
# программа должна корректно обработать эту ситуацию и не завершиться с ошибкой.


class Myexeptclass:
    def __init__(self, num_1, num_2):
        self.num_1 = num_1
        self.num_2 = num_2

    @staticmethod
    def divide(num_1, num_2):
        try:
            return num_1 / num_2
        except:
            if num_2 == 0:
                return f"На 0 делить нельзя!"
            else:
                return f"Возникла ошибка, необходимо скорректировать данные"


example_1 = Myexeptclass(100, 10)
print(Myexeptclass.divide(100, 10))
print(Myexeptclass.divide(100, 0))
print(Myexeptclass.divide(100, "r"))