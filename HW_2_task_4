# Пользователь вводит строку из нескольких слов, разделённых пробелами.
# Вывести каждое слово с новой строки.
# Строки необходимо пронумеровать.
# Если в слово длинное, выводить только первые 10 букв в слове.

user_word = input("Введите необходимые слова, в качестве разделителя используйте пробел: ")
user = []
num_list = 1
# print(user_word)
for el in range(user_word.count(' ') + 1):
    user = user_word.split()
    if len(str(user)) <= 10:
        print(f" {num_list} {user[el]}")
        num_list += 1
    else:
        print(f" {num_list} {user[el][0:10]}")
        num_list += 1
