# Реализовать формирование списка, используя функцию range() и возможности генератора.
# В список должны войти четные числа от 100 до 1000 (включая границы).
# Необходимо получить результат вычисления произведения всех элементов списка.
# Подсказка: использовать функцию reduce().


from functools import reduce

def my_func(el_i, el):
    return el_i * el

print(f"Список четный значений: \n"
      f"{[el for el in range(100, 1001) if el % 2 == 0]}")
print(f"Результат вычисления произвадных всех элементов списка: \n"
      f"{reduce(my_func, ([el for el in range(100, 1001) if el % 2 == 0]))}")

"""Написал в 2 строки - для удобства чтения"""