# Реализовать функцию int_func(), принимающую слово из маленьких латинских букв
# и возвращающую его же, но с прописной первой буквой.
# Например, print(int_func(‘text’)) -> Text.
# Продолжить работу над заданием.
# В программу должна попадать строка из слов, разделенных пробелом.
# Каждое слово состоит из латинских букв в нижнем регистре.
# Сделать вывод исходной строки, но каждое слово должно начинаться с заглавной буквы.
# Необходимо использовать написанную ранее функцию int_func().

def int_func():
    """В функции реализован небольшой функционал, она также принимает, как одно,
    так и несколько слов, разделенных пробелом
    """
    while True:
        user_list = list(input("Введите слова через пробел, для выхода введите -1: ").split(" "))
        result = []

        for el in range(len(user_list)):
            if user_list[el] == "-1":
                return False
            else:
                result.append(user_list[el].title())
        print(result)



int_func()
